package main

import (
	"fmt"
	"strings"
	/*
	 *"runtime"
	 */)

func main() {
	sentence := "Goutham is a good boy"
	fmt.Println(strings.Fields(sentence))
	/*
	 *fmt.Println("Go runs on ")
	 *switch os := runtime.GOOS; os {
	 *case "darwin":
	 *  fmt.Println("OS X")
	 *case "linux":
	 *  fmt.Println("Linux")
	 *default:
	 *  fmt.Println("%s", os)
	 *}
	 */
}
