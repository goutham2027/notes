```
# to create
pyenv virtualenv genai

# install dependencies
pip install jupyterlab
pip install openai
pip install google-cloud-aiplatform
pip install kfp


pip install -r requirements.txt

# to activate
pyenv activate genai

# start jupyter notebook
jupyter lab
```
